<%= link_to "Empty all carts", @cart, method: :delete, confirm: 'Are you sure?' %>
<%= link_to "Checkout all", new_order_path, method: :get %>
<% @carts.each do |cart| %>
  <hr>
  <%= cart.store.name %>
  <%= render :partial => "cart", :locals => {:cart => cart, :store => cart.store} %>
<% end %>